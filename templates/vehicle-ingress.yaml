{{- if .Values.vehicleIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.vehicleIngress.name }}
  namespace: {{ .Values.namespaces.vehicle }}
spec:
  ingressClassName: {{ .Values.global.ingressClassName }}
  rules:
    - http:
        paths:
          {{- range .Values.vehicleIngress.paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ .serviceName }}
                port:
                  number: {{ .servicePort }}
          {{- end }}
{{- end }}